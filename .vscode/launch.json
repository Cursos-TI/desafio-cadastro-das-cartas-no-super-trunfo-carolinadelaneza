{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Executar sem depuração",  // Nome da configuração
            "type": "cppdbg",  // Isso ainda pode ser usado, mas sem GDB
            "request": "launch",
            "program": "${workspaceFolder}/a.out",  // O caminho do seu executável
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": true
        }
    ]
}
